import { vi } from 'vitest';

export const startLogin = vi.fn();
export const handleCallback = vi.fn();
export const refreshAccessToken = vi.fn();
export const logout = vi.fn();
export const getAccessToken = vi.fn();
export const isLoggedIn = vi.fn();
